# Denylist example

Demonstrates managing a profile's denylist: add, list, update (enable/disable), and delete entries.

⚠️ Modifies a real profile. Use a test profile via `NEXTDNS_PROFILE_ID`.

## Run

```bash
NEXTDNS_API_KEY=... NEXTDNS_PROFILE_ID=abc123 go run .
```
